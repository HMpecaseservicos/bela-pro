# Configuração simplificada para Railway
[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npx prisma generate --schema=prisma/schema.prisma", "cd apps/api && npx tsc -p tsconfig.build.json"]

[start]
cmd = "node apps/api/dist/main.js"
