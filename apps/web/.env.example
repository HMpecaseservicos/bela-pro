# =============================================================================
# Variáveis de Ambiente - BELA PRO Frontend
# =============================================================================
# Copie este arquivo para .env.local (dev) ou configure no Netlify (prod)
# =============================================================================

# URL da API (Fly.io em produção)
NEXT_PUBLIC_API_URL=https://bela-pro.fly.dev/api/v1

# URL do app (para links de booking, etc)
# Em produção, será algo como: https://seu-app.netlify.app
NEXT_PUBLIC_APP_URL=http://localhost:3000
